# Session State & Handoff — repo rebuild (portfolio + blog)

## 0. Session Metadata
- **Model:** Opus 5 (`claude-opus-5`) — single model, no mid-session switch
- **Effort:** `high` (from `$CLAUDE_EFFORT`, checked not recalled)
- **Date / session #:** 2026-09-21, S1 of this repo's rebuild

## 1. Goal
Rebuild `main-portfolio` from scratch as a combined portfolio and blog on
GitHub Pages, and carry over the generally-applicable workflow apparatus from
`PeterFriedrich/cc-data-project-template`.

Owner decisions taken this session: **Astro** (chosen for zero-JS output,
markdown posts, and fast builds); **user-site root URL**, implying a repo
rename to `PeterFriedrich.github.io`; **archive the v1 site, start clean**.

## 2. Current State

### ✅ Completed
- `archive/v1-2022/` — v1 HTML/CSS/images moved intact, with a README saying
  what it is for. Not built, not linked.
- Astro 7.3.3 site: two content collections (`blog`, `projects`) with a
  zod-checked frontmatter contract; routes for home, `/blog`, `/blog/[...id]`,
  `/projects`, `/projects/[...id]`, `/about`, `/404`, `/rss.xml`, sitemap.
- Build verified: **5 pages, 2.1 s, 68 KB total, 2.5 KB home page, zero
  `<script>` tags anywhere in `dist/`.**
- Workflow apparatus ported (see the commit body of `8575962` for the
  ported / rewritten / not-ported breakdown).
- `.claude/`: `settings.json` (SessionStart + SessionEnd handoff hooks,
  a small permissions allowlist), skills `/handoff`, `/site-audit`, `/new-post`.
- Two workflows: `ci.yml` (merge gate) and `deploy.yml` (publish gate), actions
  pinned to commit SHAs resolved from the real remotes via `git ls-remote`.
- 9 tests pass; both Python doc guards exit 0.

### ⚠️ In Progress
Nothing mid-edit. Three things are done in the repo but **not done in the
world** — all three are in `TODO.md` under "Blocking the first deploy":
1. The repo is still named `main-portfolio`.
2. Pages source is presumably still "Deploy from a branch".
3. Both seeded content entries are `draft: true`, so merging to `main` today
   replaces the v1 site with an empty one.

### ❌ Blocking Issues
None in code. The above are owner actions in the GitHub UI.

## 3. Technical Learnings
- **Astro is at 7.3.3**, well past the 5.x API in the model's training. The
  content-layer API was verified against the installed package rather than
  recalled: `src/content.config.ts`, `glob()` from `astro/loaders`,
  `render(entry)` from `astro:content` (not `entry.render()`), and `entry.id`
  as the slug (not `entry.slug`).
- **`node --test tests/` fails** in Node 22.22 — it resolves `tests` as a
  module. The working form is `node --test 'tests/*.test.mjs'`, which is what
  `npm test` runs.
- **The first version of the draft test was tautological.** Flipping
  `draft: true` → `false` changed both the assertion's input and its subject,
  so it passed either way. The real check is to break the *filter* in
  `src/lib/content.ts` while leaving the frontmatter alone — done, and the test
  failed correctly with `draft "how-this-site-works" has its own page in dist/`.
  Any future change to that test should be re-validated the same way.
- `check_decisions_log.py`'s `TEST_ID` regex only matched pytest / `verify-*.js`
  spellings; it needed `[a-z0-9-]+\.test\.mjs` added or every row would have
  read "names no test".
- Doc-citation guard: `src/content` is excluded from *scanning*. Published prose
  may legitimately write `file.js:40` about someone else's code; the line-number
  ban is for pointers this repo has to keep resolving.

### Important file locations
- The draft filter, and the only place collections are queried: `src/lib/content.ts`
- Deploy state that is not in git: `docs/DEPLOY.md`
- Why anything is the way it is: `docs/DECISIONS.md` (7 rows)

## 4. Next Steps (prioritized)
1. **Owner, in the GitHub UI:** rename the repo to `PeterFriedrich.github.io`,
   then set Settings → Pages → Source → **GitHub Actions**.
2. **Before merging to `main`:** port at least one project write-up so the first
   deploy is not an empty site. `TODO.md` lists the five v1 projects and the
   four newer repos worth adding. ⚠️ Check every 2021 Drive/Omdena link — and
   **not from a Claude Code web VM**, where the egress proxy makes every
   external host look dead (`docs/REMOTE_VM.md`).
3. Rewrite `src/pages/about.astro` in the owner's voice; the current text is
   stitched from v1 and is deliberately thin.
4. Decide on tag pages — tags render as if browsable and are not.
5. Check dark-mode contrast against WCAG AA (`--fg-muted`, `--accent`).

## 5. Restoration Procedure
```bash
npm ci
git config core.hooksPath .githooks   # hooks are NOT cloned
npm run build && npm test             # build first or content tests skip
npm run guards
npm run dev                           # http://localhost:4321, drafts visible
```
Not reproducible from the repo, and not discoverable by reading code: the repo
name, the Pages source setting, and any future DNS record. All three are in
`docs/DEPLOY.md`.

---

## Update — after PR #9 merged (same session)

PR #9 merged at 21:02Z, ~4 minutes after it opened. What followed corrected two
of §2's open items and turned a third from a risk into a fact:

- **Pages source was ALREADY "GitHub Actions"** — inferred correctly from
  `configure-pages` and `deploy-pages` both succeeding on deploy run #1;
  neither does when Pages is set to "Deploy from a branch". The first deploy
  therefore published for real.
- **The owner renamed the repo to `PeterFriedrich.github.io`** and confirmed
  `curl -sI https://peterfriedrich.github.io/` → `HTTP/2 200`. The site is live
  at the root; `site` and `robots.txt` now match what is served.
- **For the ~4 minutes between merge and rename, the site was live and broken**
  — served at `/main-portfolio/`, built for the root, so every internal link
  pointed at a root that was not ours. This is exactly the failure
  `docs/DEPLOY.md` describes, observed rather than theorised. Worth keeping in
  that doc as a real incident, not a hypothetical.
- **The live site is EMPTY.** It deployed with both seeded entries still
  `draft: true`, so it shows "No projects written up yet" where the v1 site
  listed five projects. Reframed in `TODO.md` as a live fact.

⚠️ **The VM cannot reach `peterfriedrich.github.io`** — the egress proxy answers
403 to CONNECT (`connect_rejected`). Every claim about the live site above came
from the deployment record or from the owner running curl themselves. Do not
conclude the site is down from a failed request made inside this container; see
`docs/REMOTE_VM.md`.

Local checkout now tracks the renamed remote and sits on `main`. Verified the
merged work landed: `git merge-base --is-ancestor a3132a77 origin/main` → true.
